language: ruby
rvm:
- 2.1
script: bundle exec jekyll build && htmlproof ./_site
branches:
  only:
  - master
env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true
notifications:
  irc:
    channels:
      - "irc.freenode.org#4p3"